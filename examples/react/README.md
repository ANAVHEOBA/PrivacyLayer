# React Integration

A React hook and component for interacting with PrivacyLayer's privacy pool.

## Setup

```bash
npm install
npm start
```

## Files

- `usePrivacyPool.ts` — React hook wrapping deposit/withdraw/prove operations
- `PrivateTransfer.tsx` — Ready-to-use UI component
